version: '3.8'

services:
  cinema-redis:
    image: redis:alpine
    container_name: redis_cinema
    ports:
      - "6379:6379"
    restart: always

  # Nếu sau này muốn chạy MySQL trong Docker thì bỏ dấu # ở dưới
  # cinema-db:
  #   image: mysql:8.0
  #   container_name: mysql_cinema
  #   env_file: .env
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
  #     MYSQL_DATABASE: cinema_db
  #   ports:
  #     - "3306:3306"